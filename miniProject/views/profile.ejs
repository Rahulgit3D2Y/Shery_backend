<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create User</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="w-full bg-gray-900 text-white min-h-screen p-10">
    <div class="w-full flex justify-end ">
        <a class="bg-red-500 rounded-md px-2 py-2 mb-5 inline-block" href="/logout">logout</a>

    </div>
    <h1 class="text-3xl mb-5">hello, <%=user.name %>
    </h1>
    <h5 class="mb-5">you can create a new post</h5>
    <form action="/post" method="post">
        <textarea placeholder="whats on your mind"
            class="w-1/3 resize-none bg-transparent p-3 outline-none border-2 border-zinc-800 rounded-md "
            name="content" id=""></textarea>
        <input class="px-3 py-2 w-40 bg-blue-500 block rounded-md text-sm mt-2" type="submit" value="create new post">
    </form>
    <div class="posts mt-20">
        <h3 class="text-zinc-400">POSTS.</h3>
        <div class="postcontainer mt-5">
            <% user.posts.reverse().forEach( function(post) {%>
                <div class="post w-1/3 p-4 mb-4 rounded-md border-2 border-zinc-700 bg-zinc-800 ">
                    <h4 class="text-blue-500 mb-2">
                        <%= user.username %>
                    </h4>
                    <p class="text-sm tracking-tight">
                        <%=post.content %>
                    </p>
                    <div class="btns flex gap-4 mt-5">
                        <a class="text-blue-500 " href="">Like</a>
                        <a class="text-zinc-600" href="">eidt</a>
                    </div>
                </div>
                <%}); %>

        </div>
    </div>
</body>

</html>