<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create User</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="w-full bg-gray-900 text-white min-h-screen p-10">
    <div class="w-full flex justify-end ">
        <a class="bg-red-500 rounded-md px-2 py-2 mb-5 inline-block" href="/logout">logout</a>

    </div>
    <h1 class="text-3xl mb-5">hello, <%=post.user.name %>
    </h1>
    <h5 class="mb-5">edit post</h5>
    <form action="/update/<%= post._id  %>" method="post">
        <textarea placeholder="whats on your mind"
            class="w-1/3 resize-none bg-transparent p-3 outline-none border-2 border-zinc-800 rounded-md "
            name="content" id=""><%= post.content  %> </textarea>
        <input class="px-3 py-2 w-40 bg-blue-500 block rounded-md text-sm mt-2" type="submit" value="edit post">
    </form>


</body>

</html>